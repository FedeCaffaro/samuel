export const mapWallet = ({
  balance,
  chainId,
  connector,
  error,
  networkName,
  providerInfo,
  status,
  type,
  account
}) => ({
  balance,
  chainId,
  connector,
  error,
  networkName,
  providerInfo,
  status,
  type,
  account
});
